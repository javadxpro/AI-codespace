# ریاضیات (Engine/Math)

ماژول ریاضیات KIMIA — تماماً header-only و بدون وابستگی خارجی (فقط `<cmath>`).

## قراردادها

- مختصات **راست‌دست**: `+X` راست، `+Y` بالا، `−Z` جلو.
- ماتریس‌ها **ستون‌عمده** (column-major) ذخیره می‌شوند؛ اندیس `at(column, row)`.
- زاویه‌ها همیشه **رادیان** (تبدیل درجه/رادیان در `MathUtils.h`).
- کواترنیون‌ها واحد (unit) فرض می‌شوند؛ چرخش با قاعدهٔ دست راست.

## اجزا

| فایل | محتوا |
| --- | --- |
| `Vec.h` | `Vec2` / `Vec3` / `Vec4` با عملگرها، طول، نرمال‌سازی، ضرب داخلی و ضرب خارجی |
| `Mat4.h` | ماتریس ۴×۴: ضرب، ترانهاده، دترمینان، معکوس، **معکوس-ترانهاده** (نرمال‌ماتریس)، `perspective`، `lookAt`، `rotationX/Y/Z` |
| `Quat.h` | کواترنیون: از محور-زاویه، از ماتریس، ضرب، چرخش بردار، تبدیل به `Mat4` |
| `Camera.h` | دوربین پرسپکتیو: `position` / `target` / `up` + `fovY` / `aspect` / `near` / `far`؛ ماتریس‌های view و projection |
| `MathUtils.h` | `kPi`، `radians`، `degrees`، `clamp`، `lerp`، `approxEqual` |

## نکات مهم

- **نرمال‌ماتریس**: برای تبدیل نرمال‌ها در مقیاس غیریکنواخت باید از
  `inverseTranspose()` استفاده شود، نه خود ماتریس مدل (تست
  `mat4_normal_matrix_inverse_transpose`).
- **تبدیل جهت**: `transformDirection` (با `w=0`) ترنسلیشن را نادیده می‌گیرد؛
  `operator*(Vec3)` تبدیل نقطه است (`w=1`).
- `Mat4::perspective` عمق کلیپ `[−1, 1]` (سبک OpenGL) تولید می‌کند.

## تست‌ها

`Tests/src/MathTests.cpp` — شامل مقادیر دقیق (تا ۴ رقم اعشار) برای پروجکشن،
lookAt، جهت چرخش هر سه محور، رفت‌وبرگشت کواترنیون↔ماتریس و معکوس ترکیب‌ها.
