# نقشهٔ راه KIMIA

- [x] مرحلهٔ ۱: هستهٔ مستقل (Types، Log، Profiler، FixedTimeStep) و تست‌ها
- [x] مرحلهٔ ۲: ریاضیات (Vec، Mat4، Quat، Camera)
- [x] مرحلهٔ ۳: Graphics و خط لولهٔ دارایی (Primitive Meshes + OBJ/FBX/PNG/JPG/WAV/MP3)
- [x] مرحلهٔ ۴: Scene و SceneIO
- [x] مرحلهٔ ۵: Physics
- [x] مرحلهٔ ۶: Renderer (GL/EGL + رسترایزر نرم‌افزاری)
- [x] مرحلهٔ ۷: Platform (ورودی + پنجرهٔ SDL2 اختیاری)
- [x] مرحلهٔ ۸: App — Engine و WebViewer (localhost:8080)
- [x] مرحلهٔ ۹: GolfGame — بازی مرجع با نشانه‌گیری/شارژ/شوت و سازندهٔ زمین
- [x] مرحلهٔ ۱۰: KIMIA World — ویرایشگر گزینه‌محور و PLAY در مرورگر
- [x] مرحلهٔ ۱۱: سازندهٔ اجسام — زمین خالی، افزودن/جابه‌جایی/حذف/رنگ اجسام با منو، محیط، PLAY با دروازهٔ واقعی و گل
- [x] مرحلهٔ ۱۲: فرمت‌ها — OBJ+MTL با متریال/تکسچر و زیر-مش، متریال و تکسچر FBX (جاسازی‌شده هم استخراج می‌شود)، OGG/FLAC، راهنمای خروجی .blend
- [x] مرحلهٔ ۱۳: فیزیک پیشرفته ۱ — جسم‌های دینامیک (جعبه): سقوط/استراحت/چیدن روی هم، برخورد دوطرفه با توپ، هل دادن و شوت توسط بازیکن
- [x] مرحلهٔ ۱۳٫۵: فایل در صحنه — «مدل از فایل» (OBJ/FBX از پوشهٔ assets) با اندازه و نرمال‌سازی ابعاد، meshfile در SceneIO، تحمل MTL گمشده، اسپاون امن توپ (خروج از بالای جسم‌های همپوشان)
- [x] مرحلهٔ ۱۴: رندر آفلاین — ردیاب پرتو (path tracer): PBR Cook-Torrance GGX + Fresnel شلیک، روشنایی سراسری تا N جهش، آسمان + خورشید با سایهٔ نرم (قرص/مخروط)، BVH، خروجی PNG دترمینیستیک بایت-به-بایت از هر دنیای `.kimia` (ابزار `kimia_raytrace`؛ BVH و brute-force تصویر یکسان می‌دهند)
- [x] مرحلهٔ ۱۵: فیزیک پیشرفته ۲ — کنترل‌گر شخصیت (character controller): کپسول kinematic با گرانش، پرش √(2gh)، فرود روی بلوک/جعبه، برخورد-و-لغزش محوربه‌محور؛ بازیکن PLAY روی آن می‌دود
- [ ] مرحلهٔ ۱۶: فیزیک پیشرفته ۳ — اتصال‌ها (joints)
- [x] مرحلهٔ ۱۷: UI — دوربین مداری (چرخش با جهت‌ها، زوم، بازنشانی) + فهرست سلسله‌مراتبی اجسام (صفحه‌بندی ۵تایی) + بازرس زندهٔ جسم (جابه‌جایی/ارتفاع/اندازه با گام دقیق ۰٫۱، رنگ، حذف، جابه‌جایی آزاد) — همه در خود ویرایشگر گوشی
- [ ] مرحلهٔ ۱۷٫۵: گیزموها — دسته‌های جابه‌جایی/چرخش/مقیاس روی جسم انتخاب‌شده
- [ ] مرحلهٔ ۱۸: رندر بلادرنگ — تکسچر/متریال روی صحنهٔ ادیتور → نور و سایهٔ صحنه → افکت‌ها (ردیاب پرتو: نمونه‌برداری تکسچر diffuse، پریویوی تدریجی/نویز‌زدایی)

## هدف: چهار بازی روی یک موتور

**یک موتور، چند پروفایل.** همهٔ قابلیت‌ها در موتورند؛ هر بازی یک فایل پروفایل است که آن‌ها را انتخاب و تنظیم می‌کند. ترتیب ساخت و نقش هر بازی:

1. **گلف** — آزمایش و یادگیری موتور (فیزیک، منطق بازی را به چالش می‌کشد)
2. **فوتبال خیابونی ایران: کوی ابوذر** — اولین بازی جدی (فانتزی + حرکات نمایشی، ۵ در برابر ۵)
3. **زمین چمن: کوی ابوذر** — برای جذب بقیهٔ فوتبالی‌ها (فوتبال دقیق و حرفه‌ای، ۱۱ در برابر ۱۱)
4. **مسابقه واقعی: بتل گراند** — زمان زیادی لازم دارد (تقریباً مثل PUBG، بدون pay-to-win، تیم‌های ۴ نفره؛ دو حالت: **آرنا** = دو تیم ۴ نفره، **بتل گراند** = ۱۲ تیم ۴ نفره در نقشهٔ خاورمیانه و ۸ تیم ۴ نفره در نقشهٔ جنگل)

**سیاست انتشار:** هر بازی اول کامل می‌شود، بعد آنلاین منتشر می‌شود؛ تا وقتی کامل نشده نسخهٔ آفلاین در دسترس است. آنچه برای بازی قبلی ساخته شد در موتور می‌ماند و بازی بعدی از همان استفاده می‌کند و چیزهای تازه به آن اضافه می‌شود.

- [x] مرحلهٔ ۱۹: پروفایل بازی — `GameProfile` + فرمت `*.kimiaprofile` (tolerant، بایت‌یکسان)، ۴ پروفایل داخلی (street/grass/battleground/sandbox) + فایل‌های قابل‌ویرایش `Profiles/`، «دنیای جدید → کدام بازی؟»، اندازهٔ زمین/توپ/سرعت/پرش/شوت از پروفایل، محیط آسفالت، دنیا پروفایلش را در `.kimia` حمل می‌کند، فایل‌های قدیمی = sandbox با اندازهٔ زمین از Ground؛ وابستگی world→golf قطع شد
- [x] مرحلهٔ ۲۰: گلف روی KIMIA World — پروفایل `golf` (`mode shot`، `scoring hole`)، جسم «سوراخ» با قانون کاپ مرجع (۰٫۲۸ متر، < ۵ m/s)، حالت شوت (نشانه/شارژ/رها، شوت بعدی از محل توقف، ضربه‌شمار، توپ در حرکت زده نمی‌شود)، پد و عنوان‌های حالت شوت، زنجیرهٔ نشانه روی زمین، `mode`/`scoring` در فایل دنیا با سازگاری عقب‌رو؛ ۶ تست عددی از جمله شوت مرجع ۰٫۶۱ از ۱۴ متر که می‌افتد
- [x] مرحلهٔ ۲۰٫۵ (الف): زمین چندسوراخه — `Hole_1..N` به ترتیب نام، فقط سوراخ نوبت می‌گیرد، سوراخ بعدی از همان‌جا، `par` در پروفایل، جدول ضربه‌ها («۱ ۰ ۲ | جمع ۳ | پار ۹ | ۶ زیر پار»)، صفحهٔ «پایان دور» با «دور جدید/منو»، پرچم روی سوراخ نوبت، `hole i/n | total | par` در آمار؛ ۲ تست عددی
- [x] مرحلهٔ ۲۰٫۵ (ب-۱): HUD روی فریم (فونت بیت‌مپ `kimia::font`، ضربه/جمع/پار/سوراخ/جدول + نوار قدرت)، رویدادهای بازی → صدای ضربه/کاپ/گل/پایان دور (رویه‌ای، پخش در مرورگر از `/sfx/<name>`)، دوربین پشت توپ در جهت نشانه در حالت شوت؛ ۱۰ تست
- [x] مرحلهٔ ۲۰٫۵ (ب-۲): **باد** (`Wind` در فیزیک + کلید `wind` در پروفایل): شتاب افقی روی توپِ در حرکت، در هوا کامل و روی زمین ۰٫۳۵ با سقف بودجهٔ اصطکاک، هرگز روی توپ ایستاده، دترمینیستیک در ۶۰/۱۲۰ هرتز، جهت روی HUD نسبت به نشانه (`WIND 3 <-`)؛ **رکورد بهترین دور** در فایل دنیا (`# best n`، فقط دور کامل، فقط بهتر می‌شکند، `NEW BEST`/`BEST` روی HUD) — ۱۴ تست. بعد از این: **انتشار نسخهٔ آفلاین گلف**
- [x] مرحلهٔ ۲۰٫۶: **انتشار نسخهٔ آفلاین گلف** — `Tools/package_release.sh`: بیلد Release با صفر اخطار → اجرای کل سوئیت تست → بستهٔ خودکفا (باینری + پروفایل‌ها + `play.sh` یک‌دستوره + worlds/assets/licenses + `VERSION.txt` با کامیت + `MANIFEST.txt` با sha256) → **دود-آزمایی خودِ بسته** روی پورت یدکی → آرشیو `.tar.gz` (۷۸۴ کیلوبایت). به‌علاوه رفع دو ایراد CLI: `--help` پیاده شد و گزینهٔ ناشناخته دیگر بی‌صدا نادیده گرفته نمی‌شود (کد خروج ۲). بسته در پوشهٔ دیگری باز و مثل بازیکن اجرا و آزموده شد
- [x] مرحلهٔ ۲۱: چند کاراکتر — N بازیکن در فیزیک (به‌جای یک `CharacterBody`)، تیم‌ها و ترکیب از پروفایل (`team 5` / `team 11` / `team 4`)، هم‌تیمی/حریف به‌صورت بدنهٔ جامد در فیزیک (برخورد کاراکتر با کاراکتر روی هر سه محور، ایستادن روی سر)
- [x] مرحلهٔ ۲۲: مسابقه — دو دروازهٔ تیمی، امتیاز تیمی، تایمر، کیک‌آف، برد/باخت (`match.*` در پروفایل)
- [x] مرحلهٔ ۲۳: کنترل توپ — پاس/شوت هدف‌دار = همان حالت شوت گلف روی بازیکن دونده؛ توپ روی پا (دریبل)، پاس/شوت هدف‌دار با استیک مجازی، چرخش توپ + Magnus (`ball.*`)
- [x] مرحلهٔ ۲۴: آب‌وهوا و روز/شب — نور خورشید/آسمان (رندر + ردیاب پرتو)، باران/مه، باد روی توپ، سطح خیس (`weather.*`؛ مشترک هر سه بازی)
- [x] مرحلهٔ ۲۵: اسکلت و انیمیشن از FBX (ufbx) + skinning در رسترایزر نرم‌افزاری
- [x] مرحلهٔ ۲۶: حرکات نمایشی — ماشین حالت ترفند (لایی، روپایی، رولت…)، ورودی ژست روی پد لمسی، امتیاز نمایش (`tricks.*` — خیابونی روشن، چمن خاموش)
- [x] مرحلهٔ ۲۷: AI پایه — تعقیب/جاگیری/دروازه‌بان (`ai.*`)
- [x] مرحلهٔ ۲۸: HUD مسابقه (تایمر/امتیاز تیمی روی همان `kimia::font`)، دوربین پخش/سوم‌شخص (`camera.*`)، صداهای بیشتر روی همان مسیر `/sfx` (پایهٔ HUD/صدا/دوربین در ۲۰٫۵ ب-۱ ساخته شد)
- [x] مرحلهٔ ۲۹: قوانین چمن — خطا، آفساید، اوت، خستگی (پروفایل `grass`)
- [ ] مرحلهٔ ۳۰: بتل گراند (ژانر: PUBG-مانند، بدون pay-to-win) — حالت **آرنا** اول (دو تیم ۴ نفره: تیرانداز سوم‌شخص، سلاح/مهمات/جان به‌عنوان قابلیت موتور، پروفایل فقط عدد می‌دهد)، سپس حالت **بتل گراند** (۱۲ تیم ۴ نفره نقشهٔ خاورمیانه، ۸ تیم ۴ نفره نقشهٔ جنگل: نقشهٔ بزرگ با استریم، حلقهٔ جمع‌شونده، فرود، غارت)؛ پیش‌نیازهای موتور: برخورد با مدل‌ها (مش‌کالایدر)، زمین ناهموار (heightmap)، بازیکن‌های زیاد (۴۸ کاراکتر) با LOD، تصمیم مسیر نمایش (GLES بومی در برابر PNG poll)
- [ ] مرحلهٔ ۳۱: شبکه — چندنفرهٔ آنلاین (بتل گراند بدون آن معنا ندارد): سرور اختیارگر، پیش‌بینی سمت کلاینت، همگام‌سازی ۴۸ بازیکن + گلوله‌ها؛ فوتبال ۵×۵ و ۱۱×۱۱ هم روی همین لایه
